# Tasks

## Implementation

- [x] `adoptionEvidence()` builder + adopt wiring (artifacts tracked as they are created).
- [x] Adoption tasks.md updated to reflect machine-done vs human-pending items.
- [x] `verify` context-aware `Next:` (PASS variants + FAIL).
- [x] `status` required vs optional split with `· (optional)` markers.
- [x] `doctor` openspec optional line.

## Verification

- [x] 3 new tests (self-evidence, verify Next on PASS/FAIL, doctor/status optional-awareness).
- [x] Full suite: 34/34 pass.
- [x] `aief verify` at repo root passes.

## Evidence

- [x] Update evidence.md

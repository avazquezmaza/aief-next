# Tasks

## Implementation

- [x] Edit `docs/concepts.md`'s "Change Manifest" section (F3).
- [x] Edit `docs/cli.md`'s `close --evidence-from` row (F5).
- [x] Edit `docs/cli.md`'s `verify`/`verify --change` rows (F9).
- [x] Edit `docs/getting-started.md`'s root-directory guidance (project-root reinforcement).

## Documentation

- [x] (this Change IS the documentation work — no separate sub-task)

## Verification

- [x] Confirm no `cli/src/**` file was touched (`git diff --stat` shows docs only).
- [x] `node cli/bin/aief.js verify` from repo root.
- [x] `git diff --check`.

## Evidence

- [x] Update evidence.md

# Tasks

## Implementation

- [x] `skills-catalog.json`: drop `AGENTS.md`/`CLAUDE.md` from `aiRoadmap.searchFiles` (R1–R3).

## Documentation

- [x] Confirm no user doc describes `aiRoadmap`'s search files.

## Verification

- [x] Regression tests; adjust tests that relied on the self-trigger.
- [x] `npm test`, `npm run lint`, `node cli/bin/aief.js verify`, `git diff --check`.
- [ ] (review) Independent review.

## Evidence

- [x] Update evidence.md

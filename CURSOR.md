# Cursor Instructions

Follow all rules in `AGENTS.md`.

Cursor-specific guidance:

- Use the active Change as the implementation boundary.
- Prefer incremental edits over large rewrites.
- Keep generated code aligned with existing style.
- Update evidence after implementation.
- Do not duplicate `AGENTS.md`; treat it as the source of truth.
